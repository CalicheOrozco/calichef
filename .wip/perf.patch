diff --git a/.gitignore b/.gitignore
index 4ad8ccb..267be30 100644
--- a/.gitignore
+++ b/.gitignore
@@ -30,6 +30,9 @@ yarn-error.log*
 .env.*
 .env*.local
 
+# local WIP
+.wip/
+
 # vercel
 .vercel
 
diff --git a/app/collections/[id]/CollectionDetailClient.jsx b/app/collections/[id]/CollectionDetailClient.jsx
index 46c330c..1d586a4 100644
--- a/app/collections/[id]/CollectionDetailClient.jsx
+++ b/app/collections/[id]/CollectionDetailClient.jsx
@@ -175,6 +175,7 @@ export default function CollectionDetailClient({ collectionId }) {
                   alt={collection.title}
                   width={250}
                   height={250}
+                  sizes="(min-width: 768px) 250px, 60vw"
                   className="rounded-lg shadow-md"
                 />
               </div>
diff --git a/app/login/page.js b/app/login/page.js
index 5926547..27998a9 100644
--- a/app/login/page.js
+++ b/app/login/page.js
@@ -68,7 +68,14 @@ export default function Login() {
       <Navbar />
       <div className="flex items-center justify-center min-h-screen px-4 py-16">
         <div className="max-w-lg w-full panel p-8">
-          <Image src="/calichefLogo.png" alt="Logo" className="mx-auto mb-4" width={200} height={80} />
+          <Image
+            src="/calichefLogo.png"
+            alt="Logo"
+            className="mx-auto mb-4"
+            width={200}
+            height={80}
+            sizes="200px"
+          />
           
           <h2 className="text-xl text-center text-white mb-6">Welcome</h2>
           
diff --git a/components/Card.jsx b/components/Card.jsx
index 8d918f5..57174a5 100644
--- a/components/Card.jsx
+++ b/components/Card.jsx
@@ -128,12 +128,20 @@ const Card = memo(function Card({
                 alt={title}
                 width={240}
                 height={240}
+                sizes="240px"
                 className="object-cover rounded-t-xl"
               />
             </div>
           ) : (
             <div className='relative'>
-              <Image src={img_url} alt={title} width={400} height={240} className="w-full h-60 object-cover" />
+              <Image
+                src={img_url}
+                alt={title}
+                width={400}
+                height={240}
+                sizes="(max-width: 768px) 100vw, 347px"
+                className="w-full h-60 object-cover"
+              />
               <div className='pointer-events-none absolute inset-0 bg-gradient-to-t from-black/50 via-black/0 to-black/0' />
             </div>
           )}
diff --git a/components/Navbar.jsx b/components/Navbar.jsx
index a6d2588..1650c57 100644
--- a/components/Navbar.jsx
+++ b/components/Navbar.jsx
@@ -798,7 +798,14 @@ function NavbarImpl({ countRecipies }) {
       <header tabIndex='-1' className='page-header'>
         <div className='w-full flex flex-row h-16 md:h-20 bg-neutral-950/70 backdrop-blur border-b border-neutral-800 px-3 md:px-6 justify-between items-center'>
           <Link href='/' className='w-48 md:w-96' passHref>
-            <Image className='object-contain' src='/calichefLogo.png' alt='Calichef Logo' width={524} height={94} />
+            <Image
+              className='object-contain'
+              src='/calichefLogo.png'
+              alt='Calichef Logo'
+              width={524}
+              height={94}
+              sizes='(min-width: 768px) 384px, 192px'
+            />
           </Link>
 
           <div className='flex text-green-600 flex-row items-center gap-2 md:gap-4'>
@@ -1029,7 +1036,8 @@ function NavbarImpl({ countRecipies }) {
                                                         src={flagUrl}
                                                         alt={`Bandera de ${name}`}
                                                         width={20} 
-                                                        height={20} />
+                                                      height={20}
+                                                      sizes='20px' />
                                                         
                                                     )}
                                                     <span className='ml-2 text-white'>{name}</span>
@@ -1342,7 +1350,14 @@ const NavbarFallback = () => (
   <header className='page-header'>
     <div className='w-full flex flex-row h-16 md:h-20 bg-neutral-950/70 backdrop-blur border-b border-neutral-800 px-3 md:px-6 justify-between items-center'>
       <Link href='/' className='w-48 md:w-96' passHref>
-        <Image className='object-contain' src='/calichefLogo.png' alt='Calichef Logo' width={524} height={94} />
+        <Image
+          className='object-contain'
+          src='/calichefLogo.png'
+          alt='Calichef Logo'
+          width={524}
+          height={94}
+          sizes='(min-width: 768px) 384px, 192px'
+        />
       </Link>
       <div className='h-8 w-8 rounded-full bg-neutral-800 animate-pulse' aria-hidden='true' />
     </div>
diff --git a/components/Recipe.jsx b/components/Recipe.jsx
index cb0f827..9b79ba8 100644
--- a/components/Recipe.jsx
+++ b/components/Recipe.jsx
@@ -216,6 +216,7 @@ function Recipe({
               width={584} 
               height={480} 
               priority // Mark as priority since it's LCP
+              sizes="(min-width: 1024px) 584px, 100vw"
               className="block rounded-xl ring-1 ring-white/10 shadow-lg object-cover w-full max-w-lg" 
             />
           </div>
diff --git a/components/RecipeSections/Collections.jsx b/components/RecipeSections/Collections.jsx
index ccbad29..5fafe11 100644
--- a/components/RecipeSections/Collections.jsx
+++ b/components/RecipeSections/Collections.jsx
@@ -22,6 +22,7 @@ const Collections = ({ collections }) => {
                     alt={collection.name} 
                     width={80} 
                     height={80} 
+                    sizes="(min-width: 640px) 80px, 64px"
                     className='rounded-md object-cover'
                   />
                 </div>
diff --git a/components/RecipeSections/Country.jsx b/components/RecipeSections/Country.jsx
index 5e78f2f..488df3d 100644
--- a/components/RecipeSections/Country.jsx
+++ b/components/RecipeSections/Country.jsx
@@ -11,7 +11,14 @@ const Country = ({ country, countryMap }) => {
         <ul className='flex list-none flex-row justify-start items-center flex-wrap gap-2'>
           {country?.map((item, index) => (
             <li key={index} className='flex flex-col items-center'>
-              <Image className='pb-2' src={countryMap[`${item}img`]} alt={item} width={30} height={30} />
+              <Image
+                className='pb-2'
+                src={countryMap[`${item}img`]}
+                alt={item}
+                width={30}
+                height={30}
+                sizes="30px"
+              />
               <span className="text-sm sm:text-base">{countryMap[item]}</span>
             </li>
           ))}
diff --git a/components/RecipeSections/Devices.jsx b/components/RecipeSections/Devices.jsx
index 90fe91b..72c212d 100644
--- a/components/RecipeSections/Devices.jsx
+++ b/components/RecipeSections/Devices.jsx
@@ -15,6 +15,7 @@ const Devices = ({ devices, deviceImages }) => {
                   alt={item}
                   width={40}
                   height={40}
+                  sizes="(min-width: 640px) 60px, 40px"
                   className="sm:w-[60px] sm:h-[60px]"
                 />
                 <span className='text-base sm:text-lg capitalize ml-2'>{item}</span>
diff --git a/components/RecipeSections/Ingredients.jsx b/components/RecipeSections/Ingredients.jsx
index 219be76..1359d7b 100644
--- a/components/RecipeSections/Ingredients.jsx
+++ b/components/RecipeSections/Ingredients.jsx
@@ -46,6 +46,7 @@ export default function Ingredients({ ingredients, checkedIngredients, setChecke
                               alt={processedName} 
                               width={40}
                               height={40}
+                              sizes="(min-width: 640px) 56px, 40px"
                               loading="lazy" 
                               className={`object-cover ${isChecked ? 'opacity-50' : ''}`} />
                           </div>
diff --git a/components/ShoppingList.jsx b/components/ShoppingList.jsx
index 0777a77..f530a43 100644
--- a/components/ShoppingList.jsx
+++ b/components/ShoppingList.jsx
@@ -1,4 +1,3 @@
-/* eslint-disable react-hooks/rules-of-hooks */
 'use client'
 import React, { useContext, useState, useRef, useEffect, useMemo, useCallback } from 'react';
 import { CalichefContext } from '../context/MyContext';
@@ -92,7 +91,14 @@ const IngredientItem = React.memo(({ ingredient, isChecked, toggleChecked }) =>
     >
       {ingredient.image ? (
         <div className="w-10 h-10 flex items-center justify-center bg-neutral-800 rounded-full border border-neutral-700 overflow-hidden">
-          <Image src={ingredient.image} alt={ingredient.processedName || ingredient.name} width={40} height={40} className="object-cover" />
+          <Image
+            src={ingredient.image}
+            alt={ingredient.processedName || ingredient.name}
+            width={40}
+            height={40}
+            sizes="40px"
+            className="object-cover"
+          />
         </div>
       ) : (
         <div className="w-10 h-10 flex items-center justify-center bg-neutral-700 rounded-full text-neutral-400 text-xl font-bold">
@@ -148,10 +154,10 @@ const IngredientGroup = React.memo(({ group, items, recipeId, countMap, checkedI
           const ingredientKey = `${recipeId}-${group}-${idx}`;
           const isChecked = checkedIngredients[ingredientKey];
           const { processedName, displayAmount } = processIngredient(ingredient, countMap[recipeId] || 1);
-          
-          const handleIngredientToggle = useCallback(() => {
+
+          const handleIngredientToggle = () => {
             setCheckedIngredients(prev => ({ ...prev, [ingredientKey]: !prev[ingredientKey] }));
-          }, [ingredientKey]);
+          };
           
           return (
             <IngredientItem 
@@ -175,7 +181,14 @@ const RecipeCard = React.memo(({ recipe, countMap, menuOpen, toggleMenu, menuRef
     <div className="bg-neutral-800 rounded-xl p-5 shadow-md">
       <div className="flex items-center gap-4 mb-4">
         <Link href={`/${recipe.id}`} passHref>
-          <Image src={recipe.image_url} alt={recipe.title} width={70} height={70} className="rounded-lg object-cover border border-neutral-700" />
+          <Image
+            src={recipe.image_url}
+            alt={recipe.title}
+            width={70}
+            height={70}
+            sizes="70px"
+            className="rounded-lg object-cover border border-neutral-700"
+          />
         </Link>
         <Link href={`/${recipe.id}`} passHref>
           <span className="text-white text-xl font-bold tracking-tight">{recipe.title}</span>
@@ -254,7 +267,14 @@ const CategoryGroup = React.memo(({ category, items, checkedIngredients, setChec
     <div className="bg-neutral-800 rounded-xl p-5 shadow-md">
       <div className="flex items-center gap-4 mb-4">
         {IngredientCategory[category] && IngredientCategory[category][0] ? (
-          <Image src={IngredientCategory[category][0]} alt={category} width={40} height={40} className="rounded-full border border-neutral-700" />
+          <Image
+            src={IngredientCategory[category][0]}
+            alt={category}
+            width={40}
+            height={40}
+            sizes="40px"
+            className="rounded-full border border-neutral-700"
+          />
         ) : (
           <div className="w-10 h-10 flex items-center justify-center bg-neutral-700 rounded-full text-neutral-400 text-xl font-bold">🛒</div>
         )}
@@ -263,10 +283,10 @@ const CategoryGroup = React.memo(({ category, items, checkedIngredients, setChec
       <ul className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-2">
         {items.map((ingredient) => {
           const isChecked = checkedIngredients[ingredient.key];
-          
-          const handleIngredientToggle = useCallback(() => {
+
+          const handleIngredientToggle = () => {
             setCheckedIngredients(prev => ({ ...prev, [ingredient.key]: !prev[ingredient.key] }));
-          }, [ingredient.key]);
+          };
           
           return (
             <IngredientItem 
@@ -305,13 +325,6 @@ CategoryView.displayName = 'CategoryView';
 
 // Componente principal optimizado
 const ShoppingList = ({ list }) => {
-  // Un solo console.log para verificar
-  const listRef = useRef(list);
-  if (JSON.stringify(listRef.current) !== JSON.stringify(list)) {
-    console.log('ShoppingList received new list:', list?.length);
-    listRef.current = list;
-  }
-
   const contextValue = useContext(CalichefContext);
   const { originalData, setShoppingList, fetchShoppingList } = contextValue || {};
   const { updateShoppingList } = useAuth();
@@ -468,5 +481,17 @@ const ShoppingList = ({ list }) => {
 
 // Envolver todo el componente con React.memo y un comparador personalizado
 export default React.memo(ShoppingList, (prevProps, nextProps) => {
-  return JSON.stringify(prevProps.list) === JSON.stringify(nextProps.list);
-});
\ No newline at end of file
+  const prev = prevProps.list;
+  const next = nextProps.list;
+
+  if (prev === next) return true;
+  if (!Array.isArray(prev) || !Array.isArray(next)) return false;
+  if (prev.length !== next.length) return false;
+
+  for (let i = 0; i < prev.length; i++) {
+    if (prev[i]?.idRecipe !== next[i]?.idRecipe) return false;
+    if (prev[i]?.count !== next[i]?.count) return false;
+  }
+
+  return true;
+});
diff --git a/context/AuthContext.jsx b/context/AuthContext.jsx
index 5945c97..b57ea20 100644
--- a/context/AuthContext.jsx
+++ b/context/AuthContext.jsx
@@ -1,5 +1,5 @@
 'use client';
-import { createContext, useState, useEffect, useContext } from 'react';
+import { createContext, useState, useEffect, useContext, useCallback, useMemo } from 'react';
 import { useRouter } from 'next/navigation';
 
 const AuthContext = createContext();
@@ -9,7 +9,7 @@ export const AuthProvider = ({ children }) => {
   const [loading, setLoading] = useState(true);
   const router = useRouter();
 
-  const checkUserLoggedIn = async () => {
+  const checkUserLoggedIn = useCallback(async () => {
     try {
       const res = await fetch('/api/auth/me');
       const data = await res.json();
@@ -28,18 +28,18 @@ export const AuthProvider = ({ children }) => {
     } finally {
       setLoading(false);
     }
-  };
+  }, []);
 
   
-  const refreshUser = async () => {
+  const refreshUser = useCallback(async () => {
     await checkUserLoggedIn();
-  };
+  }, [checkUserLoggedIn]);
 
   useEffect(() => {
     checkUserLoggedIn();
-  }, []);
+  }, [checkUserLoggedIn]);
 
-  const login = async (email, password) => {
+  const login = useCallback(async (email, password) => {
     try {
       const res = await fetch('/api/auth/login', {
         method: 'POST',
@@ -61,9 +61,9 @@ export const AuthProvider = ({ children }) => {
     } catch (error) {
       return { success: false, message: error.message };
     }
-  };
+  }, []);
 
-  const register = async (name, email, password) => {
+  const register = useCallback(async (name, email, password) => {
     try {
       const res = await fetch('/api/auth/register', {
         method: 'POST',
@@ -83,9 +83,9 @@ export const AuthProvider = ({ children }) => {
     } catch (error) {
       return { success: false, message: error.message };
     }
-  };
+  }, []);
 
-  const logout = async () => {
+  const logout = useCallback(async () => {
     try {
       const res = await fetch('/api/auth/logout');
 
@@ -97,9 +97,9 @@ export const AuthProvider = ({ children }) => {
     } catch (error) {
       console.error('Error al cerrar sesión:', error);
     }
-  };
+  }, [router]);
 
-  const updateFavorites = async (recipeId) => {
+  const updateFavorites = useCallback(async (recipeId) => {
     if (!user) return;
 
     try {
@@ -121,19 +121,16 @@ export const AuthProvider = ({ children }) => {
       }
 
       if (res.ok && data.success) {
-        setUser({
-          ...user,
-          favorites: data.favorites
-        });
+        setUser(prev => (prev ? ({ ...prev, favorites: data.favorites }) : prev));
       } else {
         console.error('Error al actualizar favoritos:', data);
       }
     } catch (error) {
       console.error('Error al actualizar favoritos:', error);
     }
-  };
+  }, [user, router]);
 
-  const updateFavoriteCollections = async (collectionId) => {
+  const updateFavoriteCollections = useCallback(async (collectionId) => {
     if (!user) return;
 
     try {
@@ -156,21 +153,18 @@ export const AuthProvider = ({ children }) => {
       }
 
       if (res.ok && data.success) {
-        setUser({
-          ...user,
-          favoriteCollections: data.favoriteCollections
-        });
+        setUser(prev => (prev ? ({ ...prev, favoriteCollections: data.favoriteCollections }) : prev));
       } else {
         console.error('Error al actualizar colecciones favoritas:', data);
       }
     } catch (error) {
       console.error('Error al actualizar colecciones favoritas:', error);
     }
-  };
+  }, [user, router]);
 
  
 
-  const updateShoppingList = async (recipeId, action, count = 1) => {
+  const updateShoppingList = useCallback(async (recipeId, action, count = 1) => {
     if (!user) return;
   
     try {
@@ -195,19 +189,16 @@ export const AuthProvider = ({ children }) => {
       const data = await res.json();
   
       if (res.ok && data.items) {
-        setUser({
-          ...user,
-          shoppingList: data.items
-        });
+        setUser(prev => (prev ? ({ ...prev, shoppingList: data.items }) : prev));
       } else {
         console.error('Respuesta del servidor no válida:', data);
       }
     } catch (error) {
       console.error('Error al actualizar la lista de compras:', error);
     }
-  };
+  }, [user]);
   
-  const updateRecipeNotes = async (recipeId, noteContent) => {
+  const updateRecipeNotes = useCallback(async (recipeId, noteContent) => {
     if (!user) return;
     
     try {
@@ -222,13 +213,17 @@ export const AuthProvider = ({ children }) => {
       const data = await res.json();
 
       if (res.ok && data.success) {
-        setUser({
-          ...user,
-          notes: {
-            ...user.notes,
-            [recipeId]: noteContent
-          }
-        });
+        setUser(prev => (
+          prev
+            ? ({
+              ...prev,
+              notes: {
+                ...(prev.notes || {}),
+                [recipeId]: noteContent
+              }
+            })
+            : prev
+        ));
         return true;
       }
       return false;
@@ -236,25 +231,37 @@ export const AuthProvider = ({ children }) => {
       console.error('Error al actualizar las notas:', error);
       return false;
     }
-  };
-  
+  }, [user]);
   
 
+    const value = useMemo(() => ({
+      user,
+      loading,
+      login,
+      register,
+      logout,
+      updateFavorites,
+      updateFavoriteCollections,
+      updateShoppingList,
+      updateRecipeNotes,
+      isAuthenticated: !!user,
+      refreshUser,
+    }), [
+      user,
+      loading,
+      login,
+      register,
+      logout,
+      updateFavorites,
+      updateFavoriteCollections,
+      updateShoppingList,
+      updateRecipeNotes,
+      refreshUser,
+    ]);
+
   return (
     <AuthContext.Provider
-      value={{
-        user,
-        loading,
-        login,
-        register,
-        logout,
-        updateFavorites,
-        updateFavoriteCollections,
-        updateShoppingList,
-        updateRecipeNotes,
-        isAuthenticated: !!user,
-        refreshUser,
-      }}
+        value={value}
     >
       {children}
     </AuthContext.Provider>
diff --git a/context/MyContext.jsx b/context/MyContext.jsx
index bed7733..51e38cf 100644
--- a/context/MyContext.jsx
+++ b/context/MyContext.jsx
@@ -1,5 +1,5 @@
 'use client'
-import React, { createContext, useState, useEffect } from 'react'
+import React, { createContext, useState, useEffect, useCallback, useMemo } from 'react'
 import { openDB, deleteDB } from 'idb'
 import { useAuth } from './AuthContext';
 
@@ -52,7 +52,7 @@ const MyProvider = ({ children }) => {
   const [shoppingChecked, setShoppingChecked] = useState({});
   const [loadingData, setLoadingData] = useState(true)
 
-  const fetchShoppingList = async () => {
+  const fetchShoppingList = useCallback(async () => {
     try {
       const response = await fetch('/api/shopping-list');
 
@@ -63,7 +63,7 @@ const MyProvider = ({ children }) => {
     } catch (error) {
       console.error('Error al obtener la lista de compras:', error);
     }
-  };
+  }, []);
 
 
   useEffect(() => {
@@ -75,7 +75,7 @@ const MyProvider = ({ children }) => {
       fetchShoppingList();
     }
     // No hacer nada si no hay usuario
-  }, [user]);
+  }, [user, shoppingList.length, fetchShoppingList]);
 
   useEffect(() => {
     async function fetchData() {
@@ -241,7 +241,6 @@ const MyProvider = ({ children }) => {
   }, [searchTerm])
 
   useEffect(() => {
-    console.log('user:', user)
     let langPrefs = [];
     if (user) {
       if (user.languagePreferences && Array.isArray(user.languagePreferences) && user.languagePreferences.length > 0) {
@@ -255,41 +254,61 @@ const MyProvider = ({ children }) => {
     }
   }, [user])
 
+  const value = useMemo(() => ({
+    AllData,
+    setAllData,
+    collections,
+    setCollections,
+    originalData,
+    searchTerm,
+    setSearchTerm,
+    searchCollections,
+    setSearchCollections,
+    countryFilter,
+    setCountryFilter,
+    difficultyFilter,
+    setDifficultyFilter,
+    languageFilter,
+    setLanguageFilter,
+    starsFilter,
+    setStarsFilter,
+    isModalOpen,
+    setIsModalOpen,
+    categoryFilter,
+    setCategoryFilter,
+    ingredientFilter,
+    setIngredientFilter,
+    filteredCollections,
+    setFilteredCollections,
+    shoppingList,
+    setShoppingList,
+    shoppingChecked,
+    setShoppingChecked,
+    fetchShoppingList,
+    loadingData,
+  }), [
+    AllData,
+    collections,
+    originalData,
+    searchTerm,
+    searchCollections,
+    countryFilter,
+    difficultyFilter,
+    languageFilter,
+    starsFilter,
+    isModalOpen,
+    categoryFilter,
+    ingredientFilter,
+    filteredCollections,
+    shoppingList,
+    shoppingChecked,
+    fetchShoppingList,
+    loadingData,
+  ]);
+
   return (
     <CalichefContext.Provider
-      value={{
-        AllData,
-        setAllData,
-        collections,
-        setCollections,
-        originalData,
-        searchTerm,
-        setSearchTerm,
-        searchCollections,
-        setSearchCollections,
-        countryFilter,
-        setCountryFilter,
-        difficultyFilter,
-        setDifficultyFilter,
-        languageFilter,
-        setLanguageFilter,
-        starsFilter,
-        setStarsFilter,
-        isModalOpen,
-        setIsModalOpen,
-        categoryFilter,
-        setCategoryFilter,
-        ingredientFilter,
-        setIngredientFilter,
-        filteredCollections,
-        setFilteredCollections,
-        shoppingList,
-        setShoppingList,
-        shoppingChecked,
-        setShoppingChecked,
-        fetchShoppingList,
-        loadingData
-      }}
+      value={value}
     >
       {children}
     </CalichefContext.Provider>
diff --git a/next.config.mjs b/next.config.mjs
index baa4964..b913b04 100644
--- a/next.config.mjs
+++ b/next.config.mjs
@@ -17,15 +17,15 @@ const nextConfig = {
   },
   // Reduce JS bundle size
   compiler: {
-    removeConsole: process.env.NODE_ENV === 'production',
+    removeConsole: process.env.NODE_ENV === 'production'
+      ? { exclude: ['error'] }
+      : false,
     styledComponents: true,
   },
   // Ensure Turbopack is explicitly configured (silences webpack/turbopack conflict)
   turbopack: {},
-  // Enable modern Partial Prerendering (PPR) and cache components
-  experimental: {
-    cacheComponents: true,
-  },
+  // Cache Components (Next.js 16+)
+  cacheComponents: true,
 }
 
 // Configure PWA settings for better performance
